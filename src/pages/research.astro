---
import Layout from '@layouts/Layout.astro';
import profileData from '@data/profile.json';

const { research, presentations } = profileData;
---

<Layout title="Research | Tatsuhiko Akiyama" description="Research work and presentations by Tatsuhiko Akiyama">
  <div class="max-w-4xl mx-auto">
    <h1 class="text-3xl md:text-4xl font-bold mb-8 bg-gradient-to-r from-blue-600 to-purple-600 bg-clip-text text-transparent">Research</h1>
    
    <section class="mb-12 bg-white p-6 rounded-lg shadow-sm">
      <h2 class="text-2xl font-bold mb-4">Research Focus</h2>
      <p class="mb-6">My primary research focuses on methods to detect and suppress hallucinations in large language models from intermediate representations, without referring to external databases.</p>
      
      <h3 class="font-medium text-gray-700 mb-2">Research Fields</h3>
      <div class="flex flex-wrap gap-2 mb-4">
        {research.fields.map(field => (
          <span class="px-3 py-1 bg-blue-50 border border-blue-200 rounded-full text-sm text-blue-700">
            {field}
          </span>
        ))}
      </div>
    </section>
    
    <section class="mb-12">
      <h2 class="text-2xl font-bold mb-6">Presentations</h2>
      <div class="space-y-6">
        {presentations.map(presentation => (
          <div class="bg-white p-6 rounded-lg shadow-sm">
            <h3 class="font-bold text-xl mb-2">{presentation.title}</h3>
            <p class="text-gray-600 mb-4">{presentation.venue} ({presentation.year})</p>
            
            {presentation.links && presentation.links.length > 0 && (
              <div class="flex gap-4 flex-wrap">
                {presentation.links.map(link => (
                  <a 
                    href={link.url} 
                    target="_blank" 
                    rel="noopener noreferrer"
                    class="inline-flex items-center px-4 py-1 border border-gray-300 rounded-md text-sm text-gray-700 bg-white hover:bg-gray-50 transition-colors"
                  >
                    {link.name}
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 ml-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" />
                    </svg>
                  </a>
                ))}
              </div>
            )}
          </div>
        ))}
      </div>
    </section>
    
    <section class="mb-12 bg-white p-6 rounded-lg shadow-sm">
      <h2 class="text-2xl font-bold mb-4">Research Approach</h2>
      <p class="mb-4">
        My research focuses on mitigating hallucinations in large language models (LLMs) using intrinsic methods that don't rely on external knowledge bases. By analyzing the intermediate representations and uncertainty patterns within the model itself, I aim to develop techniques that can detect and suppress hallucinations in real-time.
      </p>
      <p class="mb-4">
        Key aspects of my approach include:
      </p>
      <ul class="list-disc pl-6 space-y-2 mb-4">
        <li>Uncertainty estimation in language model predictions</li>
        <li>Analyzing distribution patterns in token probabilities</li>
        <li>Developing rule-based analysis frameworks for detecting factual inconsistencies</li>
        <li>Creating confidence parameters that correlate with hallucination likelihood</li>
      </ul>
      <p>
        This research has applications in making AI systems more reliable, transparent, and trustworthy, particularly in domains where factual accuracy is critical.
      </p>
    </section>
  </div>
</Layout>